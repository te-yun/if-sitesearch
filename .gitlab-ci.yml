stages:
#- build-service
- assemble

before_script:
- touch /tmp/before.txt

after_script:
- touch /tmp/after.txt

#build-service:
#  image: openjdk:11-jre
#  stage: build-service
#  script:
#  - ./gradlew build --build-cache --info -x test
#  artifacts:
#    paths:
#    - service/build/libs/*.jar
#    expire_in: 1 month

assemble:
  variables:
    PASSWORD: some-test-password-without-quotes
  environment:
    name: main
    url: https://api.sitesearch.cloud
  services:
  - docker:dind
  image: docker:stable
  stage: assemble
  script:
  - echo PASSWORD-$PASSWORD
  - echo SERVICE_SECRET-$SERVICE_SECRET
  - cd ops/analyze-law/router
  - docker ps
  - ls -lash
  - sh build.sh